import * as React from "react";
import App from "./app";

// markup
const IndexPage = () => {
  return (
    <React.StrictMode>
      <App />
    </React.StrictMode>
  );
};

export default IndexPage;
